{
  "name": "welfare-loan-html-tracker",
  "version": "0.1.0",
  "private": true,
  "description": "Task 09 - welfare loan request tracker. Demo pipeline (npm run build) uses synthetic fixtures only. Live Gmail read-only pipeline (npm run gmail:live) mirrors into Varmen DB (VARMEN_DB_MIRROR=true) and, as of 2026-09-07, also runs daily via a Windows Scheduled Task (scripts/run-live-scheduled.ps1) - never sends, drafts stay local-only.",
  "type": "module",
  "scripts": {
    "build": "node src/run.js",
    "test": "node --test test/*.test.js",
    "gmail:auth": "node --env-file=.env src/gmail-auth.js",
    "gmail:calibrate": "node --env-file=.env src/gmail-calibrate.js",
    "gmail:live": "node --env-file=.env src/run-live.js",
    "db:migrate": "node --env-file=.env src/db-migrate.js",
    "db:migrate:002": "node --env-file=.env src/db-migrate-002.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "googleapis": "^178.0.0",
    "pg": "^8.13.1"
  }
}
