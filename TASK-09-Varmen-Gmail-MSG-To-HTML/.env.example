# Template only - copy this file to .env and fill in real values there.
# .env is gitignored and NEVER committed. Nothing in this codebase reads these
# variables yet - this is scaffolding for a future, separately-approved DB
# integration task. See docs/README.md "Varmen database" for the required
# identity check (SELECT current_database(), current_user;) that must pass
# before any write, once that task exists.

VARMEN_DB_HOST=
VARMEN_DB_PORT=
VARMEN_DB_NAME=
VARMEN_DB_USER=
VARMEN_DB_PASSWORD=
